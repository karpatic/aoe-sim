{
  "name": "aoe-sim",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:scenario": "node tools/build-scenario.mjs",
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.2.2"
  }
}
